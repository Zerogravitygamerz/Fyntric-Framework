# Fyntric Framework

This repository contains scripts for Garry's Mod.

The `lua/autorun/client/cl_inventory.lua` script adds a simple inventory UI that toggles on and off when the player presses the Tab key. The UI covers the entire screen and blurs the game in the background.

