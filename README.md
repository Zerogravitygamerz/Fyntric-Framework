# Fyntric Framework

This repository contains scripts for Garry's Mod.

The `lua/autorun/client/cl_inventory.lua` script adds a simple inventory UI that opens when the player presses the Shift key. The UI covers the entire screen and blurs the game in the background.

